package com.barclays.categories;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class CategoriesserviceApplication {

	/*
	 * 
	 * Quick code generation:
	 * 
	 * Right-click → Source → Generate Getters/Setters, Constructors, toString().
	 * 
	 * Refactoring: ctrl + Shift + F => code Format
	 * 
	 * Alt + Shift + R → rename variables/methods.
	 * 
	 * Alt + Shift + M → extract method.
	 * 
	 * Content assist / auto-complete:
	 * 
	 * Ctrl + Space suggests code completions.
	 * 
	 * Live Templates / Snippets:
	 * 
	 * Go to Window → Preferences → Java → Editor → Templates.
	 * 
	 * You can define your own “shortcuts” like sout → System.out.println();.
	 * 
	 * 
	 */
	public static void main(String[] args) {
		SpringApplication.run(CategoriesserviceApplication.class, args);
	}

}
